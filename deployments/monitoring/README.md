# Monitoring Stack — Kubernetes Manifests

This directory contains Kubernetes manifests for deploying the monitoring stack into the cluster.

## Planned components
- Prometheus deployment and ServiceMonitor resources
- Grafana deployment with pre-built dashboards
- Alertmanager with routing rules

## Status
Work in progress — manifests being tested in lab environment.
